name: Code Coverage

on:
  push:
    branches: [ main, master, develop ]
    paths:
      - src/**
      - tests/**
  workflow_dispatch:

jobs:
  coverage:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ github.workspace }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Set up Python 3.12
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-asyncio pytest-cov httpx

      - name: Run tests with coverage
        id: coverage
        run: |
          pytest tests/ -v --cov=src \
          --cov-report=html \
          --cov-report=term-missing \
          | tee pytest_output.txt
          coverage=$(grep "TOTAL" pytest_output.txt | awk '{print $4}')
          echo "coverage_percentage=$coverage" >> $GITHUB_OUTPUT

      - name: Generate Coverage Badge
        run: |
          COVERAGE=${{ steps.coverage.outputs.coverage_percentage }}
          COLOR=$(awk -v coverage=$COVERAGE 'BEGIN {
            if (coverage < 50) print "red";
            else if (coverage < 80) print "yellow";
            else print "brightgreen";
          }')
          BADGE_URL="https://img.shields.io/badge/Code_Coverage-$COVERAGE%25-$COLOR?logo=github"
          echo "BADGE_URL=$BADGE_URL" >> $GITHUB_ENV

      - name: Commit Badge to .github Folder
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git pull
          mkdir -p .github/badges
          echo "![Code Coverage]($BADGE_URL)" > .github/badges/code_coverage_badge.md
          git add .github/badges/code_coverage_badge.md
          git commit -m "Update code coverage badge in .github folder"
          git push